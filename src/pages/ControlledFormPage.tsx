import { ControlledForm } from '../components';

const ControlledFormPage = () => {
  return (
    <div className="min-h-[calc(100vh-90px)] flex justify-center items-center py-6">
      <ControlledForm />
    </div>
  );
};

export default ControlledFormPage;
