export const FILE_TYPES = ['image/png', 'image/jpeg'];
export const MAX_FILE_SIZE = 2 * 1024 * 1024;
export const PASSWORD_REGEX = {
  number: /\d/,
  uppercase: /[A-Z]/,
  lowercase: /[a-z]/,
  special: /[!@#$%^&*]/,
};
